import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const locationsReducer = createSlice({
  name: 'locationsReducer',
  initialState,
  reducers: {}
});

export const {} = locationsReducer.actions

export default locationsReducer.reducer